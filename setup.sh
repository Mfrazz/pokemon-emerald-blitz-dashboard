#!/bin/bash

# Initialize submodules (if any)
git submodule update --init --recursive

# List files in your submodule folder to confirm
echo "Contents of pokemon-assets:"
ls -R "Insert into Database/pokemon-assets"